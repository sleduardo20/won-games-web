import { screen } from '@testing-library/react';
import { renderWithTheme } from 'utils/tests/helpers';

import Wishtlist from '.';

describe('<Wishtlist/>', () => {
  it('should be able render Wishlist', () => {
    // renderWithTheme(<Wishtlist />);
  });
});
